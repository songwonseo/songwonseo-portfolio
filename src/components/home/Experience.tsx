import { Award, BriefcaseBusiness, GraduationCap } from 'lucide-react'
import { SectionTitle } from '../common/SectionTitle'
import { experience } from '../../data/experience'
import { certificates, education } from '../../data/education'
export function Experience(){return <section id="experience" className="section section--light"><div className="container"><SectionTitle eyebrow="03 / JOURNEY" title="EXPERIENCE & EDUCATION"/><div className="journey-grid"><div><h3 className="column-title"><BriefcaseBusiness/>Experience</h3>{experience.map((item)=><article className="timeline-card reveal" key={item.company}><time>{item.period}</time><h4>{item.company}</h4><strong>{item.role}</strong><p>{item.summary}</p><ul>{item.items.map((x)=><li key={x}>{x}</li>)}</ul></article>)}</div><div><h3 className="column-title"><GraduationCap/>Education</h3>{education.map((item)=><article className="timeline-card compact reveal" key={item.institution}><time>{item.period}</time><h4>{item.institution}</h4><strong>{item.title}</strong>{item.items.length>0&&<ul>{item.items.map((x)=><li key={x}>{x}</li>)}</ul>}</article>)}</div></div><div className="certificates reveal"><div className="certificates-heading"><Award/><div><span>QUALIFICATIONS</span><h3>Certificates</h3></div></div><div className="certificate-list">{certificates.map((item)=><span className="certificate-item" key={item}><Award aria-hidden="true"/><strong>{item}</strong></span>)}</div></div></div></section>}
